services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: orgalife
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - proxy

networks:
  proxy:
    external: true
